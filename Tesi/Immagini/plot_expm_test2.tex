\documentclass[tikz,border=5pt]{standalone}

\usepackage{AndreaStyle}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

\usetikzlibrary{decorations.pathreplacing}

\begin{document}
	
	\begin{tikzpicture}

		% Function definition & plotting
		\def\myfunc#1{0.2*exp(#1-2.)+0.3}
		
		\begin{axis}[
			width=8cm,
			height=5cm,
			xmin=0,
			xmax=6,
			ymin=0,
			ymax=5.5,
			xtick={1.5,3,4.5},
			ytick={0.5,2,3.5},
			xticklabels=\empty,
			yticklabels=\empty,
			axis lines=middle,,
			clip=false,
			]
			
			% Plot the curve
			\addplot[
			domain=0:5,
			samples=200,
			%thick,
			black
			]
			{\myfunc{x}};
			
			% Important points
			\pgfmathsetmacro{\expAcalx}{3.9}
			\pgfmathsetmacro{\expAcaly}{\myfunc{\expAcalx}}
				
			\pgfmathsetmacro{\expAx}{4.5}
			\pgfmathsetmacro{\expAy}{\myfunc{\expAx}}		
	
			% Other y-levels	
			\pgfmathsetmacro{\Ytrue}{2}
			\pgfmathsetmacro{\Yhaty}{\expAy+0.77}

		
			% Coordinates
			\coordinate (Acal)       at (\expAcalx,0);
			\coordinate (Acalcurve)     at (\expAcalx,\expAcaly);
			\coordinate (ExpAcal)       at (0,\expAcaly);
			
			\coordinate (YtrueCoord) at (0,\Ytrue);		
					
			\coordinate (A)     at (\expAx,0);
			\coordinate (ACurve)  at (\expAx,\expAy);
			\coordinate (ExpA)  at (0,\expAy);		
			
			\coordinate (YhatCoord)  at (0,\Yhaty);
			\coordinate (ExpHat)	 at (\expAx,\Yhaty);
		
			
			% Black dotted lines
			\draw[dashed] (A) -- (ACurve);
			\draw[dashed] (ExpA) -- (ACurve);
			\draw[densely dotted] (YhatCoord) -- (\expAx,\Yhaty);
			
			% Red dotted lines
			\draw[red,densely dotted] (Acal) -- (Acalcurve);
			\draw[red,densely dotted] (ExpAcal) -- (Acalcurve);
			
			% Dots
			\fill (ExpHat) circle (1pt);
			%\fill[red] (A) circle (1pt);
		
			% Labels on x-axis and y-axis
			\node[below, red, font=\tiny] at (Acal) {$\mathcal{A}$};
			\node[below, font=\tiny] at (A) {$A$};
					
			\node[left, xshift=1pt, font=\tiny] at (YhatCoord) {$\hat Y$};
			\node[left, xshift=2pt, font=\tiny] at (ExpA) {$\exp(A)$};
			\node[left, xshift=1pt, font=\tiny] at (YtrueCoord) {$Y_{\mathrm{true}}$};
			\node[left, xshift=2pt, red, font=\tiny] at (ExpAcal) {$\exp(\mathcal{A})$};
		
		%================================================
		% Brace and annotations
		%================================================
		
		\draw[
			decorate,
			decoration={brace, amplitude=3pt}
		] (0.15,\Yhaty) -- (0.15,\expAy);
		%	node[midway, right=2pt, align=right, font=\tiny]
		%	{absolute error};
		
		\draw[
		    decorate,
		    decoration={brace, amplitude=3pt}
		]
		   (0.11,\expAy) -- (0.11,\Ytrue)
		   node[midway, right=2pt, align=right, font=\tiny]
		   {$\mathsmaller{\approx \norm{L_{\exp}(\mathcal{A})}O(u)}$};
		     %{What we measure instead};
		     
%		\draw[
%			decorate,
%			decoration={brace, amplitude=8pt}
%		]
%			(-0.65,\Ytrue) -- (-0.65,\Yhaty);
			
		\draw
			(-0.65,\Ytrue) -- (-0.8,\Ytrue)
			-- (-0.8,\expAy-0.12) -- (-0.89,\expAy-0.02)
			-- (-0.8,\expAy+0.08) -- (-0.8, \Yhaty)
			-- (-0.6,\Yhaty);
		

		
		\end{axis}
	\end{tikzpicture}
	
\end{document}